-- Native props that already exist in GTA/FiveM and do not require streaming files.
-- Use this file to declare additional placeable items that rely on native models.

Config.Placeables.coke3 = {
    label = 'Chalumeau',
    models = { 'prop_tool_blowtorch' },
    icon = 'fa-solid fa-fire',
    interactDistance = 2.0,
    pickupLabel = 'Ramasser le chalumeau',
}
